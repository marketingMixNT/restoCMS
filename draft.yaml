models:
  Menu:
    name: json
    sort: integer nullable
    relationships:
      hasMany: Dish

  Dish:
    name: json
    ingredients: json
    price: integer
    menu_id: id
    sort: integer nullable
    relationships:
      belongsTo: Menu
